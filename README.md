# java-concepts

## Lambda Expressions
  - Syntax
    - `() -> return 1+1;`
  
  OR
    - `(int a, int b) -> return a+b;`
  OR
    - `(int a, int b) -> {
    a += 10;
    b += 20;
    return a+b;
  }`
  - 
